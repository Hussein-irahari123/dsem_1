spring.application.name=dsem-backend

# =====================
# PostgreSQL Datasource
# =====================
spring.datasource.url=${SPRING_DATASOURCE_URL:postgresql://dsem_db_user:PggsEuMH3E0wM24kx3QO7IJljGGe1WtL@dpg-d7cg5klckfvc73cavba0-a.virginia-postgres.render.com/dsem_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:dsem_db_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:PggsEuMH3E0wM24kx3QO7IJljGGe1WtL}
spring.datasource.driver-class-name=org.postgresql.Driver

# =====================
# JPA / Hibernate
# =====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# =====================
# JWT Configuration
# =====================
jwt.secret=${JWT_SECRET:dsem_super_secret_key_256bits_change_in_production_now!}
jwt.expiration=${JWT_EXPIRATION:86400000}

# =====================
# Africa's Talking (SMS)
# =====================
africas.talking.username=${SMS_USERNAME:sandbox}
africas.talking.api-key=${SMS_API_KEY:CHANGE_ME}

# =====================
# Server
# =====================
server.port=8081

# =====================
# CORS (allow React dev server)
# =====================
spring.mvc.cors.allowed-origins=http://localhost:5174
